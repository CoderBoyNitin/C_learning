// Write a C program that first reads, row by row, a 2 Dimensional array of size n × n, where n is an input
// parameter. The program should then determine whether the array just read falls into any of the following
// special cases:
// (a) Symmetric, Aij = Aji for all i, j.
// (b) Lower Triangualr, Aij = 0 when i < j.
// (c) Diagonal, Aij = 0 when i �= j.
// Also compute the sum of two diagonals

#include<stdio.h>

int main(){
    
    return 0;
}